# Architecture

This repository is being refactored from a single-purpose archive upload CLI into:

- a reusable browser automation foundation under `automation/`
- multiple business flows under `flows/`

The first migrated flow is `archive-upload`.
